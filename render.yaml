services:
  - type: web
    name: gold-forex-alert-bot
    runtime: python
    plan: free
    buildCommand: pip install -r requirements.txt
    startCommand: python forex_gold_alert_bot.py
    healthCheckPath: /health
